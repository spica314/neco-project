[package]
name = "neco-felis-compile"
version = "0.1.0"
edition = "2024"

[dependencies]
neco-felis-syn = { path = "../neco-felis-syn" }
tempfile = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[features]
has-ptx-device = []

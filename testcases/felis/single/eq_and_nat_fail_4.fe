#inductive eq : (A : Set) -> (x : A) -> A -> Prop {
    eq_refl: (A : Set) -> (x : A) -> eq A x x,
}

#inductive nat : Set {
    O: nat,
    S: nat -> nat,
}

#theorem unknown_constructor : eq nat O O {
    eq_refl nat unknown_term
}

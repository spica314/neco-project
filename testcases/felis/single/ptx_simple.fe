#entrypoint main;

#use_builtin "syscall" #as __syscall;
#use_builtin "u64" #as __u64;
#use_builtin "ctaid_x" #as __ctaid_x;
#use_builtin "ntid_x" #as __ntid_x;
#use_builtin "tid_x" #as __tid_x;

#ptx #proc kernel : () -> () {
    #let tid = __tid_x;
}

#proc main : () -> () {
    #call_ptx kernel 1 1 1 1 1 1;
    __syscall 231 0 0 0 0 0;
}
